# Copyright (c) 2024, Nestorbird_Trainee_Team and contributors
# For license information, please see license.txt

import frappe
from frappe.model.document import Document


class GymMemberName(Document):

   ''' def send_email_notification(self, email_address, message):
        frappe.sendmail(
            recipients=self.doc.email_address,
            message="We received Your details"
        )
	
    def execute(self):
        member = frappe.get_doc("Gym Member Name", doc.gym_member_name)

        for item in doc.user_details:
            item.full_name = member.full_name
            item.email_address = member.email_address
            item.contact_no = member.contact

        # create a new document
        doc = frappe.get_doc({
            'doctype': 'Task',
            'title': 'New Task'
        })
        doc.insert()'''







 

	
	
